export declare enum SpecificType {
    Product = "product",
    Collection = "collection",
    Vendor = "vendor",
    Tag = "tag"
}
export declare enum AppliedItemsType {
    AllItems = 0,
    OnlyItems = 1,
    ExcludeItems = 2
}
